#include "Point.hpp"

bool bsp (Point const a, Point const b, Point const c, Point const point);

int
main (void)
{
	const Point a;
	const Point b;
	const Point c;
	const Point point;

	bsp(a, b, c, point);
	return (0);
}
